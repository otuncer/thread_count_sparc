#include <iostream>
#include <fstream>
#include <string>

#define DEV_PATH "/devices/pseudo/thr_cnt@0:thr_cnt"

int main()
{
  string line;
  ifstream myfile(DEV_PATH);
  if(myfile.is_open())  {
    
  }

}


